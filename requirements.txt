# subscribe_server.py 배포용(Railway/Render 등). build_site.py는 여기 안 들어있다 —
# 그건 로컬/launchd(publish.sh)에서만 돌고, 이 requirements.txt가 대상으로 하는
# 상시 호스팅에는 구독 폼 수신 서버만 올라간다.
Flask>=3.0
python-dotenv>=1.0
gunicorn>=22.0
requests>=2.31
