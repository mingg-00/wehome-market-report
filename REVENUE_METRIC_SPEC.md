# 차별 지표 설계 — "위홈 실데이터 기반 예상 수익" (미구현, 설계만)

7/30 계획의 "경쟁사에 없는 차별 지표 1~2개" 항목. 8/11("신규 진입자 후크") 계획과
겹쳐서 지금은 코드로 만들지 않고 설계만 남긴다. 왜 지금 안 만드는지가 이 문서의
핵심이다.

## 왜 지금 만들면 안 되는가

RevPAR(지역별 예상 수익) = ADR(평균 숙박단가) × 점유율. 이 둘이 없으면 계산 자체가
불가능하다. 지금 우리가 가진 건 **등록 통계**(몇 곳이 영업 중인가)뿐이고, **거래
데이터**(얼마에, 며칠이나 팔렸는가)는 없다.

이 프로젝트에서 오늘 k-stay.ai의 `/analysis` 페이지를 감사하다가 정확히 이 지점의
실수를 발견했다 — 그쪽은 ADR·점유율 실측이 없으니 `generate_airbnb_sample.py`로
**70%/30% 하드코딩 난수**를 찍어 "가격/박 샘플"을 만들어 보여주고 있었다(페이지
자체에 "시뮬레이션된 데모 샘플" 면책 문구가 붙어 있음). 우리 대시보드는 그 수치를
"가짜"라고 명시적으로 경고하고 있다 — 그런데 우리가 똑같은 방식으로 가짜 수익
숫자를 만들면 그 경고 문구 자체가 자기모순이 된다. 데이터 없이 이 지표를 내놓느니
안 내놓는 게 맞다.

## 필요한 데이터와 확보 경로

| 필요 데이터 | 확보 경로 | 현재 상태 |
|---|---|---|
| 지역별 ADR(평균 숙박단가) | 위홈 예약 시스템 실거래 | ❌ `WEHOME_INTERN_API_KEY` 미발급 (기존 블로커, wehome-market-report 메모리에 기록됨) |
| 지역별 점유율 | 위홈 예약 시스템 실거래 | ❌ 위와 동일 |
| (대안) AirDNA 유료 API | 유료 구독 | ❌ 예산·계약 필요, 별도 승인 사안 |
| (대안) 전국 평균 벤치마크만 | 위홈 자체 2025.6 트러스트 리포트("호스트 연평균 매출 약 2,300만원") | ⚠️ 존재하나 전국 평균이라 지역별 차별화 불가 + 위홈 호스트로 표본 편향 + 1년 이상 지난 수치 |

## 승인되면 바로 쓸 계산식 (WEHOME_INTERN_API_KEY 발급 후)

```
지역 RevPAR = 지역 평균 ADR × 지역 평균 점유율
예상 연 수익 = 지역 RevPAR × 365 × 평균 객실수
```

`localdata.py`가 이미 구별 밀도·성장률(`saturation_signal`)을 갖고 있으니, 실거래
데이터가 들어오면 "이 구는 공급이 이만큼이고, 호스트당 예상 수익은 이 정도"라는
투자 판단형 문장을 바로 조립할 수 있다 — 지표 인프라는 이미 준비돼 있고, 막힌 건
데이터 접근권 하나뿐이다.

## 지금 당장 할 수 있는 절충안

숫자 대신 **정성적 신호**만 노출하는 안: `saturation_signal()`의 "밀도 높음 + 성장
둔화" 조합을 "포화 위험 지역", "밀도 낮음 + 성장 가파름"을 "초기 진입 기회 지역"
같은 라벨로만 보여주고, 원 단위 금액은 절대 표시하지 않는다. 이건 지금 있는 데이터로
정직하게 만들 수 있고, 8/11의 "지역 입력 → 사전 리포트" 후크에도 그대로 쓸 수 있다.

**→ 구현 완료.** `build_site.py::compute_regions`의 `tier()`(규모 사분위)×`trend()`
(직전 6개월 대비 ±15%)를 `verdict()`가 조합해 "포화 주의"·"경쟁 치열"·"성숙 시장"·
"성장 기회"·"관망 필요"·"틈새 시장"·"신규 진입 지역" 7종 라벨 중 하나로 판정한다.
`estimate.html`(지역 선택 시)과 `area/*.html`(68개 시군구 정적 페이지) 양쪽 다
결과 최상단 `verdictCard`에 원/박 금액 없이 노출 중 — 위 RevPAR 접근(실거래 데이터
필요)만 여전히 막혀 있다.
